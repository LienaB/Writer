
public class Paper {
    private String name;
    public enum Type { BOOK, COTTON, BANANA, KRAFT };
    private Type type;
    public enum Color {WHITE, YELLOW, BLACK, GREEN};
    private Color color;
}